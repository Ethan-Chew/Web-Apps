import { Box } from '@chakra-ui/react'

const RGBGuesser = () => {
    let randomRGB;

    const generateRandomRGB = () => {
        randomRGB = [Math.random() * 255, Math.random() * 255, Math.random() * 255]
    }

    return(
        <></>
    )
}
export default RGBGuesser